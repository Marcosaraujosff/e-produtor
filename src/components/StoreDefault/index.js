import React from "react";
import { Text } from 'react-native';

import { Container } from './styles';

function StoreDefault() {
    <Container>
        <Text>Loja padrão para lojas</Text>
    </Container>
}

export default StoreDefault;